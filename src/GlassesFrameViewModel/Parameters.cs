namespace GlassesFrameViewModel
{
     /// <summary>
     /// Параметры оправы для очков.
     /// </summary>
     public enum Parameters
     {
          BridgeLength,
          EndPieceLength,
          FrameWidth,
          LensFrameRadius,
          LensRadius,
          LensFrameWidth,
          LensFrameHeight
    }
}
